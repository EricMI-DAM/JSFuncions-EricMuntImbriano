// EX-15:  Explica què fa la següent funció i fes-ne un cas de prova:

// (x) => x + x + x

// La funcion toma un parametro "x" y lo suma 3 veces él mismo.
const triplicar = (x) => x + x + x;

console.log(triplicar(5)); // 15
console.log(triplicar("a")); // "aaa"
